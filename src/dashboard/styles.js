import { StyleSheet } from "react-native";

export default StyleSheet.create({
    constainer: {
        flex: 1,
        padding: 20,
        justifyContent: 'center',
        alignItems: 'center'
    }
})